---
name: media-skill
description: 多媒体生成、识别、剪辑一体化处理技能。当用户需要处理超长音视频转写文字、要求输出精确到毫秒级的时间轴（SRT/JSON）、通过多模态识别视频场景意图，或者要求进行自动化的智能视频剪辑（剔除气口、语气词、重复口语等无意义部分，分四步精剪）时，请务必使用此技能。即使他们只提到“音视频提取字幕”或“自动剪掉视频空白”，也要触发本技能。
---

# Media Skill: 多媒体生成、识别与智能剪辑一体化工作流

本技能旨在帮助用户通过一系列自动化、智能化的工作流，对音视频文件进行高精度识别、时间轴提取以及基于语义和多模态理解的智能剪辑。

## 核心能力
1. **高精度音视频转写**：将音视频转换为文字，附带精确到毫秒级的时间戳。
2. **多说话人分离**：准确标记不同说话人，处理重叠发言。
3. **多模态场景理解**：直接对视频画面和声音进行意图理解。
4. **四步智能剪辑**：从初步时间戳打底，到文字语义精剪，再到多模态深度精剪，最后综合成片合并。

## 系统配置要求
系统依赖外部 `config.txt` 文件进行配置，处理任何任务前，请先确保当前目录下存在 `config.txt` 并读取其内容：
- `MODEL_DIR`: 模型存放目录
- `OMINI_URL`: Omini 大模型 URL
- `OMINI_API_KEY`: Omini API Key
- `OMINI_MODEL_NAME`: Omini 模型名称

## 执行工作流指南

### 1. 环境与依赖检查
在开始处理之前，检查系统是否安装了 `ffmpeg` 并在环境变量中。同时确保 Python 环境已安装 `requirements.txt` 中的 `funasr`, `torch`, `modelscope` 等依赖。如果缺失，请指导用户或自动通过终端安装。

### 2. 音视频智能转写与时间轴打底
对于输入的音频/视频文件，可以通过运行 `scripts/transcribe.py` 等脚本处理：
- 使用 `ffmpeg` 提取音频（若为视频）。
- 若音频过长，内部自动分段，重采样至 16kHz。
- 调用 FunASR 的 Pipeline (`FSMN-VAD + Paraformer + CAM++`) 进行识别与说话人分离。
- **强制要求**：输出结果必须包含**精确到毫秒级的开始和结束时间戳**。生成完整的 TXT、带有毫秒级时间戳的 SRT/ASS 文件，以及包含详细段落、说话人、词/句级别时间戳的 JSON 文件。

### 3. 智能视频剪辑（4步法）
当用户请求“智能剪辑”或“精剪”时，严格执行以下 4 步策略：

#### 第一步：初步识别（时间戳打底）
- 对视频进行转写初步识别，**只识别，不剪辑**。
- 将视频中的“空白”、“气口”等无效时间点准确记录为 JSON 文件（例如 `step1_silence.json`）。

#### 第二步：文字语义精剪（切片摘取）
- 根据第一步转写的文字进行语义分析，找出语气词过多、重复、语句不通顺的片段。
- 依据时间轴使用 `ffmpeg` 对视频进行切片。
- 将这些切片存入临时文件夹，并在 JSON 中记录这些被摘取片段的时间轴（例如 `step2_semantic_clips.json`）。

#### 第三步：多模态深度精剪（降本增效）
- 对临时文件夹中的切片视频进行压缩：使用 `ffmpeg` 将其转为 540p、30帧的轻量视频，以降低大模型 Token 消耗。
- 通过读取 `config.txt` 中的 Omini 大模型配置，调用多模态大模型 API 对这些压缩切片进行识别和理解。
- 再次进行精剪判定，精准剔除重复口语、气口、无意义内容。
- **不直接物理剪辑**，将需要剔除或保留的时间点更新至 JSON 文件（例如 `step3_multimodal_filter.json`）。

#### 第四步：综合成片合并
- 结合前三步产出的所有 JSON 数据，计算并生成最终的“有效时间 JSON”。
- 根据该“有效时间 JSON”，使用 `ffmpeg` 对**原始视频**进行精确切割与合并，生成最终的成片视频素材。

## 异常处理与性能
- 并发推理时需注意缓存冲突（可通过队列或单线程处理避免）。
- 遇到解码错误应跳过该片段并记录日志，不可中断整体长视频处理。
