---
phase: 03-configuration-for-4-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.txt
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
  - MODEL-03

must_haves:
  truths:
    - "LongCat-AudioDiT config section exists in config.txt"
    - "OmniVoice config section exists in config.txt"
    - "TTS_ENGINE comment lists all 4 engine options"
  artifacts:
    - path: config.txt
      provides: "Runtime configuration for all 4 TTS engines"
      contains: "LONGCAT_"
      contains: "OMNIVOICE_"
  key_links:
    - from: config.txt
      to: scripts/tts_engines/longcat_audiodit_engine.py
      via: "config.get('LONGCAT_MODEL_NAME')"
      pattern: "LONGCAT_"
    - from: config.txt
      to: scripts/tts_engines/omnivoice_engine.py
      via: "config.get('OMNIVOICE_MODEL_NAME')"
      pattern: "OMNIVOICE_"
---

<objective>
Add LongCat-AudioDiT and OmniVoice configuration sections to config.txt, following the existing pattern from IndexTTS and Qwen3-TTS.

Purpose: Enable users to configure the two new TTS engines added in Phase 2.
Output: Updated config.txt with complete 4-model configuration.
</objective>

<execution_context>
@/Users/wuliang/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing config pattern (from config.txt lines 29-45)
# TTS_ENGINE = qwen3-tts
# INDEXTTS_MODE = local
# INDEXTTS_URL = ...
# INDEXTTS_API_KEY = ...
# INDEXTTS_MODEL_NAME = ...
# QWEN3TTS_MODE = local
# QWEN3TTS_URL = ...
# QWEN3TTS_API_KEY = ...
# QWEN3TTS_MODEL_NAME = ...

# Engine config usage (from engine implementations):
# LongCat-AudioDiT reads: LONGCAT_MODEL_NAME, LONGCAT_STEPS, LONGCAT_CFG_STRENGTH
# OmniVoice reads: OMNIVOICE_MODEL_NAME, OMNIVOICE_NUM_STEP, OMNIVOICE_GUIDANCE_SCALE
</context>

<tasks>

<task type="auto">
  <name>Add LongCat-AudioDiT config section</name>
  <files>config.txt</files>
  <action>
After the Qwen3-TTS section (around line 45), add LongCat-AudioDiT configuration:

```ini
# LongCat-AudioDiT 配置
# 注意：LongCat-AudioDiT 仅支持本地模式 (local-only)，不支持 API 调用
LONGCAT_MODEL_NAME = meituan-longcat/LongCat-AudioDiT-1B
LONGCAT_STEPS = 16
LONGCAT_CFG_STRENGTH = 4.0
```

Key points:
- NO MODE/URL/API_KEY fields (local-only engine)
- Include Chinese comment explaining local-only constraint
- Use default values from engine implementation
  </action>
  <verify>
grep -q "LONGCAT_MODEL_NAME" config.txt && grep -q "LONGCAT_STEPS" config.txt && grep -q "LONGCAT_CFG_STRENGTH" config.txt
  </verify>
  <done>LongCat-AudioDiT config section present with model name, steps, and cfg_strength fields</done>
</task>

<task type="auto">
  <name>Add OmniVoice config section</name>
  <files>config.txt</files>
  <action>
After the LongCat-AudioDiT section, add OmniVoice configuration:

```ini
# OmniVoice 配置
# 注意：OmniVoice 仅支持本地模式 (local-only)，支持 600+ 种语言的零样本声音克隆
OMNIVOICE_MODEL_NAME = k2-fsa/OmniVoice
OMNIVOICE_NUM_STEP = 32
OMNIVOICE_GUIDANCE_SCALE = 2.0
```

Key points:
- NO MODE/URL/API_KEY fields (local-only engine)
- Include Chinese comment highlighting 600+ language support
- Use default values from engine implementation
  </action>
  <verify>
grep -q "OMNIVOICE_MODEL_NAME" config.txt && grep -q "OMNIVOICE_NUM_STEP" config.txt && grep -q "OMNIVOICE_GUIDANCE_SCALE" config.txt
  </verify>
  <done>OmniVoice config section present with model name, num_step, and guidance_scale fields</done>
</task>

<task type="auto">
  <name>Update TTS_ENGINE comment with all 4 options</name>
  <files>config.txt</files>
  <action>
Update line 30 comment from:
```
# TTS_ENGINE 可选: indextts 或 qwen3-tts
```
To:
```
# TTS_ENGINE 可选: indextts, qwen3-tts, longcat-audiodit, omnivoice
```
  </action>
  <verify>
grep -q "longcat-audiodit" config.txt && grep -q "omnivoice" config.txt | head -1
  </verify>
  <done>TTS_ENGINE comment lists all 4 supported engines</done>
</task>

</tasks>

<verification>
1. config.txt contains LONGCAT_MODEL_NAME, LONGCAT_STEPS, LONGCAT_CFG_STRENGTH
2. config.txt contains OMNIVOICE_MODEL_NAME, OMNIVOICE_NUM_STEP, OMNIVOICE_GUIDANCE_SCALE
3. TTS_ENGINE comment lists all 4 engines
4. No MODE/URL/API_KEY fields for LongCat or OmniVoice (local-only)
</verification>

<success_criteria>
- LongCat-AudioDiT section added with local-only note
- OmniVoice section added with 600+ language note
- TTS_ENGINE comment updated to list all 4 options
- Config format consistent with existing IndexTTS/Qwen3-TTS sections
</success_criteria>

<output>
After completion, create `.planning/phases/03-configuration-for-4-models/03-01-SUMMARY.md`
</output>
