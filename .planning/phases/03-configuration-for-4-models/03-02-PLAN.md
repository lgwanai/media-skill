---
phase: 03-configuration-for-4-models
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config.example.txt
autonomous: true
requirements:
  - CONFIG-04

must_haves:
  truths:
    - "config.example.txt contains all 4 model configuration sections"
    - "Each model section has documentation comments explaining its purpose"
    - "Users can copy config.example.txt and fill in their values"
  artifacts:
    - path: config.example.txt
      provides: "Template configuration for new users"
      contains: "LONGCAT_"
      contains: "OMNIVOICE_"
      contains: "INDEXTTS_"
      contains: "QWEN3TTS_"
  key_links:
    - from: config.example.txt
      to: README.md
      via: "documentation reference"
      pattern: "TTS_ENGINE"
---

<objective>
Rewrite config.example.txt with complete 4-model configuration template, including documentation comments for each model's requirements and capabilities.

Purpose: Provide a comprehensive template that users can copy and customize.
Output: Updated config.example.txt with all 4 TTS engine sections.
</objective>

<execution_context>
@/Users/wuliang/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Current config.example.txt only has IndexTTS-2 section (outdated)
# Need to add: Qwen3-TTS, LongCat-AudioDiT, OmniVoice sections

# Model capabilities summary:
# - IndexTTS-2: API + Local, emotion control, high expressiveness
# - Qwen3-TTS: API + Local, emotion control, fast inference
# - LongCat-AudioDiT: Local-only, no emotion control, diffusion-based
# - OmniVoice: Local-only, no emotion control, 600+ languages
</context>

<tasks>

<task type="auto">
  <name>Rewrite config.example.txt with complete 4-model template</name>
  <files>config.example.txt</files>
  <action>
Replace the entire contents of config.example.txt with a complete template. The template MUST include:

1. **Header comment** explaining the file purpose
2. **Section 1: Basic config** (MODEL_DIR, OUTPUT_DIR)
3. **Section 2: Text LLM config** (existing - unchanged)
4. **Section 3: Multi-modal LLM config** (existing - unchanged)
5. **Section 4: Tencent COS config** (existing - unchanged)
6. **Section 5: TTS Engine Selection** - NEW section explaining the 4 options:
   ```ini
   # ---------------------------------------------------------
   # 5. TTS 引擎配置 (四引擎可选)
   # ---------------------------------------------------------
   # TTS_ENGINE 可选: indextts, qwen3-tts, longcat-audiodit, omnivoice
   # 
   # 引擎特性对比:
   # - indextts: 高情感表现力，支持情绪标签，API/本地双模式
   # - qwen3-tts: 极速推理，高音色还原，支持情绪标签，API/本地双模式
   # - longcat-audiodit: 本地扩散模型，零样本克隆，不支持情绪控制
   # - omnivoice: 支持 600+ 语言，零样本克隆，不支持情绪控制
   TTS_ENGINE = indextts
   ```

7. **Section 5.1: IndexTTS-2 config** (update comments, keep fields)
   - INDEXTTS_MODE (api/local)
   - INDEXTTS_URL (for api mode)
   - INDEXTTS_API_KEY (for api mode)
   - INDEXTTS_MODEL_NAME

8. **Section 5.2: Qwen3-TTS config** (NEW section)
   - QWEN3TTS_MODE (api/local)
   - QWEN3TTS_URL (for api mode)
   - QWEN3TTS_API_KEY (for api mode)
   - QWEN3TTS_MODEL_NAME

9. **Section 5.3: LongCat-AudioDiT config** (NEW section)
   - Note: Local-only, no API mode
   - LONGCAT_MODEL_NAME
   - LONGCAT_STEPS (inference steps)
   - LONGCAT_CFG_STRENGTH (classifier-free guidance)

10. **Section 5.4: OmniVoice config** (NEW section)
    - Note: Local-only, 600+ languages
    - OMNIVOICE_MODEL_NAME
    - OMNIVOICE_NUM_STEP (inference steps)
    - OMNIVOICE_GUIDANCE_SCALE

11. **Section 6: Emotion defaults** (existing - keep TTS_DEFAULT_* params)

Use placeholder values like `your_xxx_api_key_here` for sensitive fields.
Add Chinese comments explaining each parameter's purpose.
  </action>
  <verify>
grep -q "longcat-audiodit" config.example.txt && \
grep -q "omnivoice" config.example.txt && \
grep -q "QWEN3TTS_MODE" config.example.txt && \
grep -q "LONGCAT_MODEL_NAME" config.example.txt && \
grep -q "OMNIVOICE_MODEL_NAME" config.example.txt
  </verify>
  <done>config.example.txt contains all 4 model sections with documentation comments</done>
</task>

</tasks>

<verification>
1. config.example.txt contains TTS_ENGINE section listing all 4 options
2. IndexTTS-2 section present with MODE/URL/API_KEY/MODEL_NAME
3. Qwen3-TTS section present with MODE/URL/API_KEY/MODEL_NAME
4. LongCat-AudioDiT section present (local-only, no API fields)
5. OmniVoice section present (local-only, no API fields)
6. Documentation comments explain each model's capabilities
7. All sensitive fields use placeholder values
</verification>

<success_criteria>
- Complete 4-model template ready for users to copy
- Each engine section has clear documentation
- Local-only engines clearly marked without API fields
- Consistent formatting with existing sections
</success_criteria>

<output>
After completion, create `.planning/phases/03-configuration-for-4-models/03-02-SUMMARY.md`
</output>
